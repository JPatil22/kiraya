# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# scraped listing dumps — the old /api/ingest wrote posts + photos (with real
# phone numbers) here. The endpoint now writes to the DB, but leftover dumps
# contain third-party PII and must never be committed.
/temp-ingest/

# supabase
/supabase/.branches
/supabase/.temp
/supabase/.env
/test-results/
/playwright-report/
/playwright/.cache/
